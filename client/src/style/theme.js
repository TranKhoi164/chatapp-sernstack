import { createTheme } from '@material-ui/core'

const theme = createTheme({
  palette: {
    primary: {
      main: '#F7F8FA'
    },
    secondary: {
      main: '#757de8'
    },
    tertiary: {
      main: '#6e6b6b'
    }
  },
})

export default theme